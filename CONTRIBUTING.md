# دليل المساهمة 🌍

شكراً لاهتمامك بالمساهمة في لغة البرمجة العربية!

---

## 📋 فهرس المحتوى

- [كيفية البدء](#كيفية-البدء)
- [إعداد البيئة](#إعداد-البيئة)
- [ترتيب الفروع](#ترتيب-الفروع)
- [كتابة الكود](#كتابة-الكود)
- [إرسال التغييرات](#إرسال-التغييرات)
- [مراجعة الكود](#مراجعة-الكود)

---

## كيفية البدء

### 1. Fork المستودع

قم بفك المستودع على حسابك الشخصي.

### 2. Clone نسختك

```bash
git clone https://github.com/YOUR_USERNAME/Arabic-Programming-Language.git
cd Arabic-Programming-Language
```

### 3. إضافة المستودع الأصلي

```bash
git remote add upstream https://github.com/ORIGINAL_OWNER/Arabic-Programming-Language.git
```

---

## إعداد البيئة

### متطلبات البناء

- **C++ Compiler:** GCC 10+ / MSVC 2019+ / Clang 12+
- **CMake:** 3.16+
- **Windows SDK:** للبناء على Windows

### البناء

```bash
# Linux/macOS
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
make -j$(nproc)

# Windows (PowerShell)
mkdir build
cd build
cmake .. -G "Visual Studio 16 2019"
cmake --build . --config Release
```

---

## ترتيب الفروع

نستخدم نموذج Git Flow:

```
main          ← الإصدار الرسمي
  ↑
develop       ← تطوير الميزات
  ↑
feature/*    ← ميزات جديدة
bugfix/*     ← إصلاح أخطاء
docs/*       ← توثيق
```

### إنشاء فرع جديد

```bash
# ميزة جديدة
git checkout -b feature/new-feature

# إصلاح خطأ
git checkout -b bugfix/issue-123

# توثيق
git checkout -b docs/improve-readme
```

---

## كتابة الكود

### معايير الكود

1. **التعليقات:**
   - اكتب التعليقات بالعربية أو الإنجليزية
   - وضّح الغرض من كل دالة
   - استخدم docstrings للدوال العامة

2. **التسمية:**
   - المتغيرات: `camelCase` أو `_snake_case`
   - الدوال: `snake_case` أو أفعال عربية
   - الصفوف: `PascalCase` أو أسماء عربية

3. **المسافة البادئة:**
   - استخدم 4 مسافات (أو tab)

### مثال على التعليقات

```cpp
// ============================================================
// دالة جمع عددين
// Param: أ - العدد الأول
// Param: ب - العدد الثاني
// Return: مجموع أ و ب
// ============================================================
int جمع(int أ, int ب) {
    return أ + ب;
}
```

### اختبار الكود

```bash
# تشغيل الاختبارات
./arabic_compiler --test

# اختبار ميزة معينة
./arabic_compiler --test lexer
```

---

## إرسال التغييرات

### 1. Commit التغييرات

```bash
git add .
git commit -m "إضافة: ميزة جديدة للغة"
```

### رسالة Commit

```
نوع: وصف مختصر

وصف تفصيلي (اختياري)

Fixes: #123
See also: #456
```

**أنواع Commit:**

| النوع | الوصف |
|-------|-------|
| `إضافة` | ميزة جديدة |
| `تعديل` | تغيير ميزة موجودة |
| `إصلاح` | إصلاح خطأ |
| `تحسين` | تحسين أداء أو كود |
| `توثيق` | توثيق فقط |
| `اختبار` | إضافة اختبارات |

### 2. Push للفرع

```bash
git push origin feature/my-feature
```

### 3. فتح Pull Request

1. اذهب إلى صفحة المستودع
2. اضغط "New Pull Request"
3. اختر الفرع
4. اكتب وصفاً واضحاً
5. اضغط "Create Pull Request"

---

## مراجعة الكود

### معايير المراجعة

- ✅ الكود يعمل بشكل صحيح
- ✅ اجتياز جميع الاختبارات
- ✅ لا توجد تحذيرات compiler
- ✅ التعليقات واضحة وكافية
- ✅ يتبع معايير الكود

### نموذج مراجعة

```
## الوصف
[وصف موجز للتغييرات]

## التحقق
- [ ] الميزات الجديدة مجربة
- [ ] الاختبارات تمر
- [ ] التوثيق محدّث
- [ ] لا توجد مشاكل أمنية

## ملاحظات
[أي ملاحظات إضافية]
```

---

## 🔒 سياسة الأمان

- ⚠️ لا تستخدم `system()` بدون تحقق
- ⚠️ تحقق من جميع مدخلات المستخدم
- ⚠️ تجنب Buffer Overflows
- ⚠️ استخدم parameterized queries للبيانات

---

## 📝 ملاحظات مهمة

1. **الاحترام:** عامل جميع المساهمين باحترام
2. **الصبر:** المراجعة قد تأخذ بعض الوقت
3. **التعاون:** اسأل إذا كان لديك أي استفسار
4. **التوثيق:** حدّث التوثيق مع تغييراتك

---

## 📞 للتواصل

- **Issues:** استخدم صفحة Issues في GitHub
- **المناقشات:** استخدم GitHub Discussions
- **البريد:** [email@example.com]

---

<div align="center">

**شكراً لمساهمتك! 🌟**

</div>
