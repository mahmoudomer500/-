# مكتبة المدخلات والمخرجات العربية
# توفر دوال للتعامل مع الملفات والإدخال/الإخراج

دالة اطبع_سطر(نص):
    اطبع(نص)
    اطبع("")
نهاية

دالة اقرأ_سطر():
    # في النسخة الحقيقية، ستقرأ من الإدخال
    ارجع "إدخال تجريبي"
نهاية

دالة افتح_ملف(اسم_الملف، وضع):
    # محاكاة فتح ملف
    متغير معرف_ملف = "ملف_" + اسم_الملف
    ارجع معرف_ملف
نهاية

دالة اقرأ_من_ملف(معرف_ملف):
    # محاكاة قراءة من ملف
    ارجع "محتوى الملف"
نهاية

دالة اكتب_في_ملف(معرف_ملف، نص):
    # محاكاة كتابة في ملف
    اطبع("كتابة في الملف: " + نص)
نهاية

دالة اغلق_ملف(معرف_ملف):
    # محاكاة إغلاق ملف
    اطبع("تم إغلاق الملف: " + معرف_ملف)
نهاية

دالة حجم_الملف(معرف_ملف):
    # محاكاة حجم الملف
    ارجع 1024
نهاية