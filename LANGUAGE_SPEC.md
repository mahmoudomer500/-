# مواصفات لغة البرمجة العربية
# Arabic Programming Language Specification
# Version: 1.0.0-beta

---

## 📋 فهرس المحتوى

1. [المقدمة](#المقدمة)
2. [الكلمات المفتاحية](#الكلمات-المفتاحية)
3. [أنواع البيانات](#أنواع-البيانات)
4. [المتغيرات](#المتغيرات)
5. [العمليات](#العمليات)
6. [الشروط](#الشروط)
7. [الحلقات](#الحلقات)
8. [الدوال](#الدوال)
9. [الصفوف (OOP)](#الصفوف-oop)
10. [المصفوفات](#المصفوفات)
11. [النصوص](#النصوص)
12. [المدخلات والمخرجات](#المدخلات-والمخرجات)
13. [إدارة الأخطاء](#إدارة-الأخطاء)
14. [المكتبات والاستيرادات](#المكتبات-والاستيرادات)
15. [قواعد التسمية](#قواعد-التسمية)
16. [الأولوية](#الأولوية)

---

## المقدمة

### معلومات عامة

- **الاسم:** لغة البرمجة العربية
- **الامتداد:** `.عربي`
- **نظام التشغيل:** Windows (أساسي)
- **المترجم:** C++ مع توليد x64

### الخصائص الرئيسية

- ✅ برمجة بالكامل بالعربية
- ✅ نظام ذاتي (Self-Hosting)
- ✅ OOP كامل (صفوف، وراثة)
- ✅ توليد PE (ملفات تنفيذية)
- ✅ RTL natively

---

## الكلمات المفتاحية

### تعريفات أساسية

| الكلمة | الإنجليزية | الوصف |
|--------|-----------|-------|
| `مت` | `var` | تعريف متغير |
| `ثابت` | `const` | تعريف ثابت |
| `دالة` | `function` | تعريف دالة |
| `صف` | `class` | تعريف صف |
| `عام` | `public` | видимость عام |
| `خاص` | `private` | видимость خاص |
| `محمي` | `protected` | видимость محمي |
| `ينتمي_إلى` | `extends` | وراثة |
| `يُنفذ` | `implements` | implements interface |
| `مجرّد` | `abstract` | صف مجرد |

### التحكم

| الكلمة | الإنجليزية | الوصف |
|--------|-----------|-------|
| `اذا` | `if` | شرط |
| `وإلا` | `else` | شرط بديل |
| `او_اذا` | `elif` | شرط آخر |
| `بينما` | `while` | حلقة while |
| `لكل` | `for` | حلقة for |
| `في` | `in` | في (لحلقة for) |
| `اقطع` | `break` | قطع الحلقة |
| `تابع` | `continue` | متابعة الحلقة |
| `ارجع` | `return` | إرجاع قيمة |
| `اخرج` | `exit` | خروج من البرنامج |

### العمليات

| الكلمة | الإنجليزية | الوصف |
|--------|-----------|-------|
| `جديد` | `new` | إنشاء كائن |
| `هذا` | `this` | المرجع الحالي |
| `ابناء` | `children` | أبناء الصنف |
| `حذف` | `delete` | حذف كائن |
| `باطل` | `void` | بدون إرجاع |

### المنطقية

| الكلمة | الإنجليزية | الوصف |
|--------|-----------|-------|
| `و` | `&&` | و (منطقي) |
| `او` | `\|\|` | أو (منطقي) |
| `ليس` | `!` | ليس (نفي) |
| `صحيح` | `true` | صحيح |
| `خطأ` | `false` | خطأ |

### المخرجات

| الكلمة | الإنجليزية | الوصف |
|--------|-----------|-------|
| `اطبع` | `print` | طباعة |
| `ادمج` | `input` | إدخال |
| `ملف` | `file` | ملف |
| `اقرأ` | `read` | قراءة |
| `اكتب` | `write` | كتابة |

---

## أنواع البيانات

### الأنواع الأساسية

| النوع | الوصف | الحجم | مثال |
|-------|-------|-------|------|
| `رقم` | عدد صحيح | 32-bit | `مت س: رقم = 42` |
| `عشري` | عدد عشري | 64-bit | `مت س: عشري = 3.14` |
| `نص` | نص | متغير | `مت س: نص = "مرحبا"` |
| `منطقي` | صح/خطأ | 1-byte | `مت س: منطقي = صحيح` |

### أنواع متقدمة

| النوع | الوصف | مثال |
|-------|-------|------|
| `مصفوفة` | مجموعة عناصر | `[]` |
| `شيء` | كائن | `جديد صنف()` |
| `فارغ` | لا شيء | `ثابت س: فارغ` |

### التحويل بين الأنواع

```arabic
// تحويل رقم إلى نص
مت س = 42
مت نص = س.إلى_نص()

// تحويل نص إلى رقم
مت نص = "123"
مت س = نص.إلى_رقم()

// تحويل عشري إلى رقم
مت س = 3.99
مت رقم = س.إلى_رقم()  // = 3
```

---

## المتغيرات

### التعريف البسيط

```arabic
مت اسم = "أحمد"
مت age = 25
مت is_student = صحيح
```

### التعريف مع النوع

```arabic
مت اسم: نص = "أحمد"
مت age: رقم = 25
مت height: عشري = 1.75
مت is_valid: منطقي = صحيح
```

### الثوابت

```arabic
ثابت PI = 3.14159
ثابت MAX_SIZE = 1000
ثابت APP_NAME = "لغة البرمجة العربية"
```

### قواعد المتغيرات

1. ✅ يجب تعريف المتغير قبل استخدامه
2. ✅ الأسماء حساسة لحالة الأحرف
3. ✅ لا يمكن إعادة تعريف متغير موجود
4. ✅ يمكن تعديل قيمة المتغير بعد التعريف

---

## العمليات

### العمليات الحسابية

| العملية | الوصف | مثال | النتيجة |
|--------|-------|------|---------|
| `+` | جمع | `5 + 3` | `8` |
| `-` | طرح | `5 - 3` | `2` |
| `*` | ضرب | `5 * 3` | `15` |
| `/` | قسمة | `15 / 3` | `5` |
| `%` | باقي القسمة | `17 % 5` | `2` |
| `^` | أس | `2 ^ 3` | `8` |

### عمليات المقارنة

| العملية | الوصف | مثال |
|--------|-------|------|
| `==` | يساوي | `5 == 5` → `صحيح` |
| `!=` | لا يساوي | `5 != 3` → `صحيح` |
| `>` | أكبر من | `5 > 3` → `صحيح` |
| `<` | أصغر من | `5 < 3` → `خطأ` |
| `>=` | أكبر أو يساوي | `5 >= 5` → `صحيح` |
| `<=` | أصغر أو يساوي | `5 <= 3` → `خطأ` |

### العمليات المنطقية

```arabic
مت a = صحيح
مت b = خطأ

// و (and)
مت result1 = a و b          // خطأ

// أو (or)
مت result2 = a او b         // صحيح

// ليس (not)
مت result3 = ليس b          // صحيح
```

---

## الشروط

### if البسيط

```arabic
دالة فحص_العمر(عمر):
    اذا عمر >= 18:
        اطبع("بالغ")
    نهاية
نهاية
```

### if و else

```arabic
دالة فحص_الرقم(رقم):
    اذا رقم > 0:
        اطبع("إيجابي")
    وإلا:
        اطبع("سلبي أو صفر")
    نهاية
نهاية
```

### if و else if و else

```arabic
دالة درجة_الطالب(درجة):
    اذا درجة >= 90:
        اطبع("ممتاز")
    او_اذا درجة >= 80:
        اطبع("جيد جداً")
    او_اذا درجة >= 70:
        اطبع("جيد")
    او_اذا درجة >= 60:
        اطبع("مقبول")
    وإلا:
        اطبع("راسب")
    نهاية
نهاية
```

### الشروط المتداخلة

```arabic
دالة فحص_النوع(عمر، هو_طالب):
    اذا عمر >= 18:
        اذا هو_طالب:
            اطبع("طالب جامعي")
        وإلا:
            اطبع("بالغ عامل")
    وإلا:
        اطبع("قاصر")
    نهاية
نهاية
```

---

## الحلقات

### حلقة while

```arabic
مت i = 0
بينما i < 10:
    اطبع(i)
    i = i + 1
نهاية
```

### حلقة for (لكل)

```arabic
// التكرار على مصفوفة
لكل item في [1, 2, 3, 4, 5]:
    اطبع(item)
نهاية

// التكرار مع index
لكل i، item في المصفوفة:
    اطبع(i + ": " + item)
نهاية
```

### حلقة التكرار بعدد

```arabic
لكل i في النطاق(0, 10):
    اطبع(i)
نهاية
```

### التحكم في الحلقات

```arabic
مت i = 0
بينما i < 100:
    i = i + 1
    
    اذا i == 50:
        اطبع("وصلنا للنصف!")
        اقطع    // يخرج من الحلقة
    
    اطبع(i)
نهاية

// continue example
لكل i في النطاق(0, 10):
    اذا i % 2 == 0:
        تابع    // يskip التكرار الحالي
    اطبع(i + " عدد فردي")
نهاية
```

---

## الدوال

### تعريف دالة بسيطة

```arabic
دالة مرحبا():
    اطبع("مرحباً بالعالم!")
نهاية
```

### دالة مع معاملات

```arabic
دالة جمع(أ، ب):
    أرجع أ + ب
نهاية

دالة اطبع_الاسم(اسم):
    اطبع("مرحباً يا " + اسم)
نهاية
```

### دالة مع نوع الإرجاع

```arabic
دالة ضرب(أ: رقم، ب: رقم): رقم
    أرجع أ * ب
نهاية

دالة اطبع_النص(نص: نص): فارغ
    اطبع(نص)
نهاية
```

### معاملات افتراضية

```arabic
دالة مرحبا(اسم = "عالم"):
    اطبع("مرحباً يا " + اسم)
نهاية

مرحبا()              // مرحباً يا عالم
مرحبا("أحمد")       // مرحباً يا أحمد
```

### دوال مجهولة (Lambda)

```arabic
مت مربع = دالة(س): س * س
مت نتيجة = مربع(5)  // 25
```

### التحميل الزائد

```arabic
دالة اطبع(نص: نص):
    // طباعة نص
نهاية

دالة اطبع(رقم: رقم):
    // طباعة رقم
نهاية

اطبع("مرحبا")   // يستدعي النسخة الأولى
اطبع(42)        // يستدعي النسخة الثانية
```

---

## الصفوف (OOP)

### تعريف صف بسيط

```arabic
صف نقطة:
    خاص:
        رقم س
        رقم ص
    
    عام:
        دالة جديد(س، ص):
            هذا.س = س
            هذا.ص = ص
        
        دالة المسافة():
            أرجع sqrt(هذا.س * هذا.س + هذا.ص * هذا.ص)
نهاية
```

### إنشاء كائن

```arabic
مت p = جديد نقطة(3, 4)
مت المسافة = p.المسافة()
```

### المُنشئ (Constructor)

```arabic
صف شخص:
    خاص:
        نص اسم
        رقم عمر
    
    عام:
        دالة جديد(اسم، عمر):
            هذا.اسم = اسم
            هذا.عمر = عمر
        
        دالة اطبع_البيانات():
            اطبع(هذا.اسم + " - " + هذا.عمر)
نهاية
```

### الوراثة (Inheritance)

```arabic
// الصف الأب
صف شكل:
    عام:
        دالة المساحة(): رقم
            أرجع 0
        
        دالة المحيط(): رقم
            أرجع 0

// الصف الابن
صف مربع inherits_from شكل:
    خاص:
        رقم طول
    
    عام:
        دالة جديد(طول):
            هذا.طول = طول
        
        دالة المساحة(): رقم
            أرجع هذا.طول * هذا.طول
        
        دالة المحيط(): رقم
            أرجع 4 * هذا.طول
نهاية
```

### تعدد الأشكال

```arabic
دالة اطبع_المساحة(ش: شكل):
    اطبع(ش.المساحة())
نهاية

مت م = جديد مربع(5)
اطبع_المساحة(م)  // يطبع 25
```

### الصفوف المجردة

```arabic
صف مجرد شكل_مجرّد:
    عام:
        دالة مجرّد احسب_المساحة(): رقم
        دالة احسب_المحيط(): رقم

صف دائرة inherits_from شكل_مجرّد:
    خاص:
        رقم نصف_القطر
    
    عام:
        دالة جديد(نق):
            هذا.نصف_القطر = نق
        
        دالة احسب_المساحة(): رقم
            أرجع 3.14159 * هذا.نصف_القطر * هذا.نصف_القطر
        
        دالة احسب_المحيط(): رقم
            أرجع 2 * 3.14159 * هذا.نصف_القطر
نهاية
```

---

## المصفوفات

### إنشاء مصفوفة

```arabic
مت ارقام = [1, 2, 3, 4, 5]
مت نصوص = ["أحمد", "محمد", "علي"]
مت مصفوفة_فارغة = []
```

### الوصول للعناصر

```arabic
مت ارقام = [10, 20, 30, 40, 50]
مت اول = ارقام[0]     // 10
مت ثاني = ارقام[1]    // 20
مت الاخير = ارقام[-1] // 50
```

### تعديل العناصر

```arabic
مت ارقام = [1, 2, 3]
ارقام[0] = 10      // [10, 2, 3]
ارقام.أضف(4)       // [10, 2, 3, 4]
```

### دوال المصفوفات

```arabic
مت ارقام = [3, 1, 4, 1, 5, 9, 2, 6]

ارقام.أضف(7)           // إضافة عنصر
ارقام.إزالة(0)          // إزالة عنصر
مت الطول = طول(ارقام)   // 8
مت مجموع = ارقام.مجموع()    // مجموع العناصر
مت أكبر = ارقام.أكبر()     // أكبر عنصر
مت أصغر = ارقام.أصغر()     // أصغر عنصر
مت فرز = ارقام.فرز()       // [1, 1, 2, 3, 4, 5, 6, 9]
مت بحث = ارقام.يحتوي(5)    // صحيح
```

### التكرار على المصفوفات

```arabic
مت ارقام = [1, 2, 3, 4, 5]

لكل رقم في ارقام:
    اطبع(رقم)
نهاية

// مع index
لكل i، رقم في ارقام:
    اطبع(i + ": " + رقم)
نهاية
```

---

## النصوص

### إنشاء نص

```arabic
مت نص1 = "مرحبا"
مت نص2 = 'عالم'
مت نص3 = """
هذا نص
متعدد
الأسطر
"""
```

### العمليات على النصوص

```arabic
مت نص = "مرحبا يا عالم"

مت الطول = طول(نص)              // 15
مت نص_كبير = نص.كبير()           // "مرحبا يا عالم" (حروف كبيرة)
مت نص_صغير = نص.صغير()           // "مرحبا يا عالم" (حروف صغيرة)
مت نص_فرز = نص.فرز()             // ["عالم", "مرحبا", "يا"]
مت نص_بديل = نص.بديل("يا", "بـ") // "مرحبا بـ عالم"
مت نص_بحث = نص.يحتوي("عالم")     // صحيح
```

### تنسيق النصوص

```arabic
مت اسم = "أحمد"
مت عمر = 25

// باستخدام +
مت رسالة = "الاسم: " + اسم + "، العمر: " + عمر

// باستخدام تنسيق
مت رسالة = "الاسم: {1}، العمر: {2}".تنسيق(اسم، عمر)
```

---

## المدخلات والمخرجات

### الطباعة

```arabic
اطبع("مرحبا")              // نص
اطبع(42)                  // رقم
اطبع(صحيح)                // منطقي
اطبع(أ، ب)                // متعدد
اطبع("العدد: " + 5)      // نص مع رقم
```

### الإدخال

```arabic
مت اسم = ادمج("أدخل اسمك: ")
مت عمر = ادمج("أدخل عمرك: ")
مت رقم = ادمج_رقم("أدخل رقماً: ")
```

### الملفات

```arabic
// قراءة ملف
مت محتوى = اقرأ_ملف("test.txt")

// كتابة ملف
اكتب_ملف("output.txt", "محتوى جديد")

// ملحق للملف
ملحق_ملف("log.txt", "سجل جديد")
```

---

## إدارة الأخطاء

### TRY و CATCH

```arabic
جرب:
    مت نتيجة = 10 / 0
اصطدام خطأ:
    اطبع("حدث خطأ: " + خطأ.الرسالة)
نهاية
```

### TRY و CATCH و FINALLY

```arabic
جرب:
    مت ملف = افتح_ملف("test.txt")
    // معالجة الملف
اصطدام خطأ:
    اطبع("فشل في فتح الملف")
اخيراً:
    // هذا الكود يُنفذ دائماً
    اطبع("انتهى")
نهاية
```

### رمي أخطاء

```arabic
دالة قسمة(أ، ب):
    اذا ب == 0:
        ارمي خطأ("لا يمكن القسمة على صفر")
    أرجع أ / ب
نهاية
```

---

## المكتبات والاستيرادات

### استيراد مكتبة

```arabic
استورد "رياضيات"      // مكتبة الرياضيات
استورد "نصوص"         // مكتبة النصوص
استورد "ملفات"        // مكتبة الملفات
```

### استيراد مخصص

```arabic
استورد "مكتبتي.عربي"
```

### استخدام المكتبة

```arabic
استورد "رياضيات"

مت جذر = رياضيات.جذر_تربيعي(16)    // 4
مت SIN = رياضيات.جيب(0)              // 0
```

---

## قواعد التسمية

### المتغيرات والدوال

| النوع | القاعدة | مثال |
|-------|---------|------|
| متغير | camelCase أو snake_case | `اسم_المستخدم`، `اسمالمستخدم` |
| دالة | snake_case | `اطبع_الاسم()` |
| ثابت | UPPER_SNAKE_CASE | `MAX_SIZE` |

### الصفوف

| النوع | القاعدة | مثال |
|-------|---------|------|
| صف | PascalCase | `نقطة`, `شخص` |
| واجهة | PascalCase + Interface | `شكل_واجهة` |

### الكلمات المحجوزة

```
مت، ثابت، دالة، صف، عام، خاص، محمي
اذا، وإلا، او_اذا، بينما، لكل، في
ارجع، اقطع، تابع، اخرج
صحيح، خطأ، فارغ
```

---

## الأولوية

### ترتيب العمليات (من الأعلى للأقل)

```
1. الأقواس                    ()
2. دوال وخصائص               () [] .
3. ليس                      !
4. * / %                     ضرب، قسمة، باقي
5. + -                       جمع، طرح
6. < <= > >=                 مقارنات
7. == !=                     تساوي
8. و                         &&
9. او                        ||
10. =                        إسناد
```

### أمثلة

```arabic
// 1 + 2 * 3 = 7 (ليس 9)
مت نتيجة = 1 + 2 * 3

// (1 + 2) * 3 = 9
مت نتيجة = (1 + 2) * 3

// صحيح و ليس خطأ = صحيح
مت نتيجة = صحيح و ليس خطأ
```

---

## أمثلة شاملة

### مثال 1: آلة حاسبة

```arabic
دالة آلة_حاسبة():
    اطبع("=== آلة حاسبة ===")
    
    مت أ = ادمج_رقم("العدد الأول: ")
    مت ب = ادمج_رقم("العدد الثاني: ")
    
    اطبع("النتيجة:")
    اطبع("  الجمع: " + (أ + ب))
    اطبع("  الطرح: " + (أ - ب))
    اطبع("  الضرب: " + (أ * ب))
    
    اذا ب != 0:
        اطبع("  القسمة: " + (أ / ب))
    وإلا:
        اطبع("  القسمة: لا يمكن")
    نهاية
نهاية

آلة_حاسبة()
```

### مثال 2: صف مع وراثة

```arabic
صف شكل:
    عام:
        نص اسم
        
        دالة جديد(اسم):
            هذا.اسم = اسم
        
        دالة اطبع_الاسم():
            اطبع(هذا.اسم)

صف دائرة inherits_from شكل:
    خاص:
        رقم نصف_القطر
    
    عام:
        دالة جديد(اسم، نق):
            هذا.اسم = اسم
            هذا.نصف_القطر = نق
        
        دالة المساحة():
            أرجع 3.14159 * هذا.نصف_القطر * هذا.نصف_القطر

// الاستخدام
مت د = جديد دائرة("دائرة صغيرة", 5)
د.اطبع_الاسم()
اطبع("المساحة: " + د.المساحة())
```

### مثال 3: تكرار على مصفوفة

```arabic
دالة بحث_خطي(مصفوفة، هدف):
    لكل i، عنصر في مصفوفة:
        اذا عنصر == هدف:
            أرجع i
        نهاية
    نهاية
    أرجع -1
نهاية

مت ارقام = [10, 20, 30, 40, 50]
مت موقع = بحث_خطي(ارقام, 30)
اطبع("الموقع: " + موقع)  // 2
```

---

## ملاحظات ختامية

### القيود الحالية

- ⚠️ لا يوجد GC (Garbage Collection) كامل
- ⚠️ لا يوجد مدير حزم رسمي
- ⚠️ Bootstrap يحتاج اختبار

### خطط مستقبلية

- [ ] مدير حزم (Package Manager)
- [ ] مكتبة شبكات محسّنة
- [ ] IDE متكامل
- [ ] Debugger
- [ ] JIT Compilation

---

<div align="center">

**نهاية المواصفات**
**لغة البرمجة العربية - الإصدار 1.0.0-beta**

</div>
