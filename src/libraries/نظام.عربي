# 🖥️ مكتبة النظام العربية - الإصدار 1.0
# ════════════════════════════════════════════════════════════════════
# 
# وصف المكتبة:
#   مكتبة للتفاعل مع نظام التشغيل
#   تتضمن: الوقت، الانتظار، متغيرات البيئة
#
# الإصدار: 1.0
# التاريخ: Sprint D
# ════════════════════════════════════════════════════════════════════

# ────────────────────────────────────────────────────────────────────
# 1️⃣  دالة الوقت
# ────────────────────────────────────────────────────────────────────

دالة وقت():
    "الحصول على الطابع الزمني الحالي (Unix Timestamp)"
    # يتطلب دعم داخلي
    أرجع الوقت_الحالي()

# ────────────────────────────────────────────────────────────────────
# 2️⃣  دالة الانتظار (Sleep)
# ────────────────────────────────────────────────────────────────────

دالة انتظار(مللي_ثانية):
    "إيقاف التنفيذ لفترة زمنية"
    # يتطلب دعم داخلي
    أرجع انتظر(مللي_ثانية)

# ────────────────────────────────────────────────────────────────────
# 3️⃣  دالة متغيرات البيئة
# ────────────────────────────────────────────────────────────────────

دالة بيئة(اسم):
    "الحصول على قيمة متغير بيئة"
    # يتطلب دعم داخلي
    أرجع احصل_بيئة(اسم)

# ────────────────────────────────────────────────────────────────────
# 4️⃣  دالة تنفيذ أمر نظام
# ────────────────────────────────────────────────────────────────────

دالة نفذ(أمر):
    "تنفيذ أمر نظام خارجي"
    # يتطلب دعم داخلي
    أرجع نفذ_أمر(أمر)
