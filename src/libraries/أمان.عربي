# 🔐 مكتبة الأمان العربية المتقدمة
# ════════════════════════════════════════════════════════════════════
#
# وصف المكتبة:
#   مكتبة شاملة للأمان والتشفير في البرمجة العربية
#   تدعم: التشفير، فك التشفير، التجزئة، المصادقة، والأمان العام
#
# الإصدار: 1.0 - متقدمة
# التاريخ: ديسمبر 2025
# الحالة: مكتملة ومتقدمة
# ════════════════════════════════════════════════════════════════════

# ────────────────────────────────────────────────────────────────────
# 1️⃣  إعداد وتهيئة الأمان
# ────────────────────────────────────────────────────────────────────

# متغير مدير الأمان العام
متغير مدير_الأمان = 0

# دالة تهيئة نظام الأمان
دالة تهيئة_الأمان()
    # إنشاء مدير الأمان
    مدير_الأمان = إنشاء_مدير_أمان()

    # تهيئة جميع خوارزميات الأمان
    إذا مدير_الأمان != 0
        إذا تهيئة_جميع_خوارزميات_الأمان(مدير_الأمان)
            اطبع("تم تهيئة نظام الأمان بنجاح")
            ارجع صحيح
        وإلا
            اطبع("فشل في تهيئة نظام الأمان")
            ارجع خطأ
        نهاية
    وإلا
        اطبع("فشل في إنشاء مدير الأمان")
        ارجع خطأ
    نهاية
نهاية

# ────────────────────────────────────────────────────────────────────
# 2️⃣  التشفير وفك التشفير
# ────────────────────────────────────────────────────────────────────

# دالة الحصول على معالج التشفير
دالة احصل_على_معالج_التشفير(خوارزمية)
    إذا مدير_الأمان != 0
        ارجع إنشاء_معالج_تشفير(مدير_الأمان, خوارزمية)
    وإلا
        ارجع 0
    نهاية
نهاية

# دالة تشفير نص
دالة شفر_النص(النص, المفتاح, خوارزمية)
    متغير معالج = احصل_على_معالج_التشفير(خوارزمية)
    إذا معالج != 0
        متغير نص_مشفر = تشفير_النص_بالمعالج(معالج, النص, المفتاح)
        ارجع نص_مشفر
    وإلا
        ارجع ""
    نهاية
نهاية

# دالة فك تشفير نص
دالة فك_شفر_النص(النص_المشفر, المفتاح, خوارزمية)
    متغير معالج = احصل_على_معالج_التشفير(خوارزمية)
    إذا معالج != 0
        متغير نص_مفكوك = فك_تشفير_النص_بالمعالج(معالج, النص_المشفر, المفتاح)
        ارجع نص_مفكوك
    وإلا
        ارجع ""
    نهاية
نهاية

# دالة تشفير متقدم مع متجه التهيئة
دالة شفر_متقدم(النص, المفتاح, متجه_التهيئة, خوارزمية)
    متغير معالج = احصل_على_معالج_التشفير(خوارزمية)
    إذا معالج != 0
        متغير نص_مشفر = تشفير_متقدم_بالمعالج(معالج, النص, المفتاح, متجه_التهيئة)
        ارجع نص_مشفر
    وإلا
        ارجع ""
    نهاية
نهاية

# دالة فك تشفير متقدم
دالة فك_شفر_متقدم(النص_المشفر, المفتاح, متجه_التهيئة, خوارزمية)
    متغير معالج = احصل_على_معالج_التشفير(خوارزمية)
    إذا معالج != 0
        متغير نص_مفكوك = فك_تشفير_متقدم_بالمعالج(معالج, النص_المشفر, المفتاح, متجه_التهيئة)
        ارجع نص_مفكوك
    وإلا
        ارجع ""
    نهاية
نهاية

# ────────────────────────────────────────────────────────────────────
# 3️⃣  التجزئة (Hashing)
# ────────────────────────────────────────────────────────────────────

# دالة الحصول على معالج التجزئة
دالة احصل_على_معالج_التجزئة(خوارزمية)
    إذا مدير_الأمان != 0
        ارجع إنشاء_معالج_تجزئة(مدير_الأمان, خوارزمية)
    وإلا
        ارجع 0
    نهاية
نهاية

# دالة تجزئة نص
دالة تجزئة_النص(النص, خوارزمية)
    متغير معالج = احصل_على_معالج_التجزئة(خوارزمية)
    إذا معالج != 0
        متغير تجزئة = أنشئ_تجزئة_للنص(معالج, النص)
        ارجع تجزئة
    وإلا
        ارجع ""
    نهاية
نهاية

# دالة تجزئة ملف
دالة تجزئة_الملف(مسار_الملف, خوارزمية)
    متغير معالج = احصل_على_معالج_التجزئة(خوارزمية)
    إذا معالج != 0
        متغير تجزئة = أنشئ_تجزئة_للملف(معالج, مسار_الملف)
        ارجع تجزئة
    وإلا
        ارجع ""
    نهاية
نهاية

# دالة التحقق من التجزئة
دالة تحقق_التجزئة(النص, التجزئة_المتوقعة, خوارزمية)
    متغير تجزئة_فعلية = تجزئة_النص(النص, خوارزمية)
    ارجع تجزئة_فعلية == التجزئة_المتوقعة
نهاية

# ────────────────────────────────────────────────────────────────────
# 4️⃣  المصادقة والتحقق
# ────────────────────────────────────────────────────────────────────

# دالة الحصول على معالج المصادقة
دالة احصل_على_معالج_المصادقة(خوارزمية)
    إذا مدير_الأمان != 0
        ارجع إنشاء_معالج_مصادقة(مدير_الأمان, خوارزمية)
    وإلا
        ارجع 0
    نهاية
نهاية

# دالة إنشاء توقيع رقمي
دالة أنشئ_توقيع_رقمي(البيانات, المفتاح_الخاص, خوارزمية)
    متغير معالج = احصل_على_معالج_المصادقة(خوارزمية)
    إذا معالج != 0
        متغير توقيع = أنشئ_توقيع_رقمي_بالمعالج(معالج, البيانات, المفتاح_الخاص)
        ارجع توقيع
    وإلا
        ارجع ""
    نهاية
نهاية

# دالة التحقق من التوقيع الرقمي
دالة تحقق_التوقيع_الرقمي(البيانات, التوقيع, المفتاح_العام, خوارزمية)
    متغير معالج = احصل_على_معالج_المصادقة(خوارزمية)
    إذا معالج != 0
        متغير صحيح = تحقق_التوقيع_الرقمي_بالمعالج(معالج, البيانات, التوقيع, المفتاح_العام)
        ارجع صحيح
    وإلا
        ارجع خطأ
    نهاية
نهاية

# دالة إنشاء زوج مفاتيح
دالة أنشئ_زوج_مفاتيح(خوارزمية)
    متغير معالج = احصل_على_معالج_المصادقة(خوارزمية)
    إذا معالج != 0
        متغير زوج_مفاتيح = أنشئ_زوج_مفاتيح_بالمعالج(معالج)
        ارجع زوج_مفاتيح
    وإلا
        ارجع {}
    نهاية
نهاية

# ────────────────────────────────────────────────────────────────────
# 5️⃣  إدارة كلمات المرور
# ────────────────────────────────────────────────────────────────────

# دالة تجزئة كلمة مرور
دالة تجزئة_كلمة_المرور(كلمة_المرور)
    # استخدام bcrypt أو scrypt للتجزئة الآمنة
    متغير تجزئة = تجزئة_كلمة_مرور_آمنة(كلمة_المرور)
    ارجع تجزئة
نهاية

# دالة التحقق من كلمة المرور
دالة تحقق_كلمة_المرور(كلمة_المرور, التجزئة_المحفوظة)
    متغير تجزئة_جديدة = تجزئة_كلمة_المرور(كلمة_المرور)
    ارجع تجزئة_جديدة == التجزئة_المحفوظة
نهاية

# دالة توليد كلمة مرور عشوائية
دالة توليد_كلمة_مرور_عشوائية(الطول)
    متغير كلمة_مرور = أنشئ_كلمة_مرور_عشوائية(الطول)
    ارجع كلمة_مرور
نهاية

# دالة تقييم قوة كلمة المرور
دالة قيم_قوة_كلمة_المرور(كلمة_المرور)
    متغير نقاط_القوة = 0

    # التحقق من الطول
    إذا طول(كلمة_المرور) >= 8
        نقاط_القوة = نقاط_القوة + 1
    نهاية
    إذا طول(كلمة_المرور) >= 12
        نقاط_القوة = نقاط_القوة + 1
    نهاية

    # التحقق من وجود أحرف كبيرة
    متغير يحتوي_على_كبيرة = خطأ
    لكل حرف في كلمة_المرور
        إذا حرف >= 'A' و حرف <= 'Z'
            يحتوي_على_كبيرة = صحيح
            نقاط_القوة = نقاط_القوة + 1
            اخرج
        نهاية
    نهاية

    # التحقق من وجود أحرف صغيرة
    متغير يحتوي_على_صغيرة = خطأ
    لكل حرف في كلمة_المرور
        إذا حرف >= 'a' و حرف <= 'z'
            يحتوي_على_صغيرة = صحيح
            نقاط_القوة = نقاط_القوة + 1
            اخرج
        نهاية
    نهاية

    # التحقق من وجود أرقام
    متغير يحتوي_على_رقم = خطأ
    لكل حرف في كلمة_المرور
        إذا حرف >= '0' و حرف <= '9'
            يحتوي_على_رقم = صحيح
            نقاط_القوة = نقاط_القوة + 1
            اخرج
        نهاية
    نهاية

    # التحقق من وجود رموز خاصة
    متغير يحتوي_على_رمز = خطأ
    متغير الرموز = "!@#$%^&*()_+-=[]{}|;:,.<>?"
    لكل حرف في كلمة_المرور
        إذا يحتوي_على(الرموز, حرف)
            يحتوي_على_رمز = صحيح
            نقاط_القوة = نقاط_القوة + 1
            اخرج
        نهاية
    نهاية

    # تحديد مستوى القوة
    إذا نقاط_القوة >= 5
        ارجع "قوية جداً"
    وإلا إذا نقاط_القوة >= 4
        ارجع "قوية"
    وإلا إذا نقاط_القوة >= 3
        ارجع "متوسطة"
    وإلا
        ارجع "ضعيفة"
    نهاية
نهاية

# ────────────────────────────────────────────────────────────────────
# 6️⃣  إدارة الجلسات والمصادقة
# ────────────────────────────────────────────────────────────────────

# دالة إنشاء رمز جلسة
دالة أنشئ_رمز_جلسة()
    متغير رمز = أنشئ_رمز_جلسة_عشوائي()
    ارجع رمز
نهاية

# دالة تشفير رمز الجلسة
دالة شفر_رمز_الجلسة(رمز_الجلسة, مفتاح_التطبيق)
    متغير رمز_مشفر = شفر_النص(رمز_الجلسة, مفتاح_التطبيق, "AES")
    ارجع رمز_مشفر
نهاية

# دالة فك تشفير رمز الجلسة
دالة فك_شفر_رمز_الجلسة(رمز_مشفر, مفتاح_التطبيق)
    متغير رمز = فك_شفر_النص(رمز_مشفر, مفتاح_التطبيق, "AES")
    ارجع رمز
نهاية

# دالة التحقق من صحة الجلسة
دالة تحقق_صحة_الجلسة(رمز_الجلسة_المشفر, مفتاح_التطبيق)
    متغير رمز = فك_شفر_رمز_الجلسة(رمز_الجلسة_المشفر, مفتاح_التطبيق)
    إذا رمز != ""
        # التحقق من أن الرمز لم ينتهِ صلاحيته
        متغير صالح = تحقق_من_صلاحية_الرمز(رمز)
        ارجع صالح
    وإلا
        ارجع خطأ
    نهاية
نهاية

# ────────────────────────────────────────────────────────────────────
# 7️⃣  حماية البيانات
# ────────────────────────────────────────────────────────────────────

# دالة تشفير ملف
دالة شفر_الملف(مسار_الملف_الأصلي, مسار_الملف_المشفر, المفتاح, خوارزمية)
    متغير نجح = تشفير_ملف_كامل(مسار_الملف_الأصلي, مسار_الملف_المشفر, المفتاح, خوارزمية)
    ارجع نجح
نهاية

# دالة فك تشفير ملف
دالة فك_شفر_الملف(مسار_الملف_المشفر, مسار_الملف_المفكوك, المفتاح, خوارزمية)
    متغير نجح = فك_تشفير_ملف_كامل(مسار_الملف_المشفر, مسار_الملف_المفكوك, المفتاح, خوارزمية)
    ارجع نجح
نهاية

# دالة توليد مفتاح عشوائي
دالة توليد_مفتاح_عشوائي(طول_المفتاح, خوارزمية)
    متغير مفتاح = أنشئ_مفتاح_عشوائي(طول_المفتاح, خوارزمية)
    ارجع مفتاح
نهاية

# دالة توليد متجه تهيئة
دالة توليد_متجه_تهيئة(طول_المتجه)
    متغير متجه = أنشئ_متجه_تهيئة_عشوائي(طول_المتجه)
    ارجع متجه
نهاية

# ────────────────────────────────────────────────────────────────────
# 8️⃣  أدوات الأمان المساعدة
# ────────────────────────────────────────────────────────────────────

# دالة تنظيف البيانات الحساسة من الذاكرة
دالة نظف_البيانات_الحساسة(البيانات)
    # الكتابة فوق البيانات بقيم عشوائية
    متغير بيانات_منظفة = املأ_بالعشوائي(البيانات)
    ارجع بيانات_منظفة
نهاية

# دالة التحقق من سلامة البيانات
دالة تحقق_سلامة_البيانات(البيانات, التجزئة_المتوقعة)
    متغير تجزئة_فعلية = تجزئة_النص(البيانات, "SHA256")
    ارجع تجزئة_فعلية == التجزئة_المتوقعة
نهاية

# دالة إنشاء شهادة أمان
دالة أنشئ_شهادة_أمان(البيانات, التجزئة)
    متغير شهادة = {
        "البيانات": البيانات,
        "التجزئة": التجزئة,
        "الوقت": احصل_على_الوقت_الحالي(),
        "الإصدار": "1.0"
    }
    ارجع شهادة
نهاية

# ────────────────────────────────────────────────────────────────────
# 9️⃣  أمثلة الاستخدام
# ────────────────────────────────────────────────────────────────────

# مثال التشفير الأساسي
دالة مثال_التشفير_الأساسي()
    إذا تهيئة_الأمان()
        متغير النص_الأصلي = "هذا نص سري يجب تشفيره"
        متغير المفتاح = "مفتاح_التشفير_السري_123"

        متغير نص_مشفر = شفر_النص(النص_الأصلي, المفتاح, "AES")
        إذا نص_مشفر != ""
            اطبع("النص المشفر: " + نص_مشفر)

            متغير نص_مفكوك = فك_شفر_النص(نص_مشفر, المفتاح, "AES")
            إذا نص_مفكوك == النص_الأصلي
                اطبع("تم فك التشفير بنجاح!")
                ارجع صحيح
            وإلا
                اطبع("فشل في فك التشفير")
                ارجع خطأ
            نهاية
        وإلا
            اطبع("فشل في التشفير")
            ارجع خطأ
        نهاية
    وإلا
        اطبع("فشل في تهيئة الأمان")
        ارجع خطأ
    نهاية
نهاية

# مثال التجزئة
دالة مثال_التجزئة()
    إذا تهيئة_الأمان()
        متغير النص = "كلمة المرور السرية"
        متغير تجزئة = تجزئة_النص(النص, "SHA256")

        إذا تجزئة != ""
            اطبع("تجزئة SHA256: " + تجزئة)

            متغير صحيح = تحقق_التجزئة(النص, تجزئة, "SHA256")
            إذا صحيح
                اطبع("تم التحقق من التجزئة بنجاح")
                ارجع صحيح
            وإلا
                اطبع("فشل في التحقق من التجزئة")
                ارجع خطأ
            نهاية
        وإلا
            اطبع("فشل في إنشاء التجزئة")
            ارجع خطأ
        نهاية
    وإلا
        اطبع("فشل في تهيئة الأمان")
        ارجع خطأ
    نهاية
نهاية

# مثال إدارة كلمات المرور
دالة مثال_كلمات_المرور()
    إذا تهيئة_الأمان()
        متغير كلمة_المرور = "كلمة_مرور_قوية_123!"

        متغير قوة = قيم_قوة_كلمة_المرور(كلمة_المرور)
        اطبع("قوة كلمة المرور: " + قوة)

        متغير تجزئة = تجزئة_كلمة_المرور(كلمة_المرور)
        إذا تجزئة != ""
            اطبع("تم تجزئة كلمة المرور بنجاح")

            متغير صحيح = تحقق_كلمة_المرور(كلمة_المرور, تجزئة)
            إذا صحيح
                اطبع("تم التحقق من كلمة المرور بنجاح")
                ارجع صحيح
            وإلا
                اطبع("فشل في التحقق من كلمة المرور")
                ارجع خطأ
            نهاية
        وإلا
            اطبع("فشل في تجزئة كلمة المرور")
            ارجع خطأ
        نهاية
    وإلا
        اطبع("فشل في تهيئة الأمان")
        ارجع خطأ
    نهاية
نهاية

# مثال المصادقة الرقمية
دالة مثال_المصادقة_الرقمية()
    إذا تهيئة_الأمان()
        متغير البيانات = "هذه البيانات تحتاج توقيع رقمي"
        متغير زوج_المفاتيح = أنشئ_زوج_مفاتيح("RSA")

        إذا زوج_المفاتيح != {}
            متغير المفتاح_الخاص = احصل_على_المفتاح_الخاص(زوج_المفاتيح)
            متغير المفتاح_العام = احصل_على_المفتاح_العام(زوج_المفاتيح)

            متغير التوقيع = أنشئ_توقيع_رقمي(البيانات, المفتاح_الخاص, "RSA")
            إذا التوقيع != ""
                اطبع("تم إنشاء التوقيع الرقمي")

                متغير صحيح = تحقق_التوقيع_الرقمي(البيانات, التوقيع, المفتاح_العام, "RSA")
                إذا صحيح
                    اطبع("تم التحقق من التوقيع الرقمي بنجاح")
                    ارجع صحيح
                وإلا
                    اطبع("فشل في التحقق من التوقيع الرقمي")
                    ارجع خطأ
                نهاية
            وإلا
                اطبع("فشل في إنشاء التوقيع الرقمي")
                ارجع خطأ
            نهاية
        وإلا
            اطبع("فشل في إنشاء زوج المفاتيح")
            ارجع خطأ
        نهاية
    وإلا
        اطبع("فشل في تهيئة الأمان")
        ارجع خطأ
    نهاية
نهاية

# ────────────────────────────────────────────────────────────────────
# 🔟  ثوابت وإعدادات الأمان
# ────────────────────────────────────────────────────────────────────

# خوارزميات التشفير
متغير AES = "AES"
متغير DES = "DES"
متغير TRIPLE_DES = "3DES"
متغير BLOWFISH = "BLOWFISH"

# خوارزميات التجزئة
متغير MD5 = "MD5"
متغير SHA1 = "SHA1"
متغير SHA256 = "SHA256"
متغير SHA512 = "SHA512"

# خوارزميات المصادقة
متغير RSA = "RSA"
متغير DSA = "DSA"
متغير ECDSA = "ECDSA"

# أحجام المفاتيح
متغير مفتاح_128_بت = 128
متغير مفتاح_192_بت = 192
متغير مفتاح_256_بت = 256

# أطوال المتجهات
متغير متجه_16_بايت = 16
متغير متجه_24_بايت = 24
متغير متجه_32_بايت = 32
