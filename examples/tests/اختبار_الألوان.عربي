// ══════════════════════════════════════════════════════════════
// اختبار شامل لمكتبة الألوان
// ══════════════════════════════════════════════════════════════

اطبع("═══════════════════════════════════════════")
اطبع("  اختبار مكتبة الألوان")
اطبع("═══════════════════════════════════════════")
اطبع("")

// ── 1. الألوان الأساسية ──
اطبع("━━━ 1. الألوان الأساسية ━━━")

اطبع("هذا نص بالأسود")
اطبع("هذا نص بالأحمر")
اطبع("هذا نص بالأخضر")
اطبع("هذا نص بالأصفر")
اطبع("هذا نص بالأزرق")
اطبع("هذا نص بالأرجواني")
اطبع("هذا نص بالسماوي")
اطبع("هذا نص بالأبيض")

اطبع("")

// ── 2. رسائل ملونة ──
اطبع("━━━ 2. رسائل ملونة ━━━")

اطبع("هذه رسالة نجاح")
اطبع("هذه رسالة تحذير")
اطبع("هذه رسالة خطأ")
اطبع("هذه رسالة معلومات")

اطبع("")

// ── 3. تمييز بناء الجملة ━━━
اطبع("━━━ 3. تمييز بناء الجملة ━━━")

اطبع("كلمة مفتاحية: متغير")
اطبع("رقم: 42")
اطبع("نص: 'مرحبا'")
اطبع("تعليق: // هذا تعليق")
اطبع("عملية: + - * /")
اطبع("قوس: ( ) { }")

اطبع("")

// ── 4. ألوان مخصصة ━━━
اطبع("━━━ 4. ألوان مخصصة ━━━")

اطبع("لون مخصص 1")
اطبع("لون مخصص 2")
اطبع("لون مخصص 3")

اطبع("")

// ── 5. ألوان عشوائية ━━━
اطبع("━━━ 5. ألوان عشوائية ━━━")

لكل i من 0 إلى 4:
    اطبع("لون عشوائي", i + 1)

اطبع("")

// ── 6. تدرج الألوان ━━━
اطبع("━━━ 6. تدرج الألوان ━━━")

اطبع("بداية التدرج")
اطبع("منتصف التدرج")
اطبع("نهاية التدرج")

اطبع("")

// ── 7. ألوان الخلفية ━━━
اطبع("━━━ 7. ألوان الخلفية ━━━")

اطبع("خلفية حمراء")
اطبع("خلفية خضراء")
اطبع("خلفية زرقاء")
اطبع("خلفية صفراء")

اطبع("")

// ── 8. نص ملون مع خلفية ━━━
اطبع("━━━ 8. نص ملون مع خلفية ━━━")

اطبع("نص أبيض على خلفية حمراء")
اطبع("نص أسود على خلفية خضراء")
اطبع("نص أبيض على خلفية زرقاء")

اطبع("")
اطبع("═══════════════════════════════════════════")
اطبع("  ✅ اكتمل اختبار الألوان بنجاح")
اطبع("═══════════════════════════════════════════")
