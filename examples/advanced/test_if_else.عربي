# اختبار بسيط للشروط

متغير درجة = 85

اطبع("=== اختبار الشروط ===")
إذا درجة >= 90
    اطبع("درجة ممتازة!")
وإلا إذا درجة >= 80
    اطبع("درجة جيدة جداً!")
وإلا إذا درجة >= 70
    اطبع("درجة جيدة!")
وإلا
    اطبع("يحتاج تحسين!")
نهاية

اطبع("=== نهاية الاختبار ===")
