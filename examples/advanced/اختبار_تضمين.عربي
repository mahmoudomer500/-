# اختبار نظام التضمين

# تضمين المكتبة المساعدة
تضمين "مكتبة_مساعدة.عربي"

# الآن يمكننا استخدام المتغيرات من المكتبة
اطبع("بدء اختبار التضمين...")
اطبع("إصدار المكتبة: " + إصدار_المكتبة)

اطبع("✅ اختبار التضمين نجح!")
