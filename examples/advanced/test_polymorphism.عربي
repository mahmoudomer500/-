# اختبار تعدد الأشكال والوراثة المتقدمة

صنف حيوان
    دالة تكلم()
        اطبع("الحيوان يصدر صوتاً")
    نهاية
نهاية

صنف كلب يمتد حيوان
    دالة تكلم()
        اطبع("الكلب ينبح: هو هو!")
    نهاية
نهاية

صنف قطة يمتد حيوان
    دالة تكلم()
        اطبع("القطة تموء: مياو!")
    نهاية
نهاية

# اختبار استدعاء الدوال المتعددة الأشكال
اطبع("--- اختبار تعدد الأشكال ---")

متغير حيواني = جديد حيوان()
متغير كلبي = جديد كلب()
متغير قطتي = جديد قطة()

حيواني.تكلم()
كلبي.تكلم()
قطتي.تكلم()

# اختبار الوراثة واستدعاء الأب
اطبع("--- اختبار استدعاء الأب ---")

صنف كلب_بوليسي يمتد كلب
    دالة تكلم()
        الأب.تكلم()
        اطبع("الكلب البوليسي مستعد للخدمة!")
    نهاية
نهاية

متغير بوليسي = جديد كلب_بوليسي()
بوليسي.تكلم()

# ----------------------------------

صنف حيوان
    دالة تكلم()
        اطبع("الحيوان يصدر صوتاً")
    نهاية
نهاية

صنف كلب يمتد حيوان
    دالة تكلم()
        اطبع("الكلب ينبح: هو هو!")
    نهاية
نهاية

صنف قطة يمتد حيوان
    دالة تكلم()
        اطبع("القطة تموء: مياو!")
    نهاية
نهاية

# دالة تأخذ كائناً وتستدعي دالة تكلم()
دالة اجعل_الحيوان_يتكلم(ح)
    ح.تكلم()
نهاية

اطبع("--- اختبار تعدد الأشكال ---")
متغير كلبي = جديد كلب()
متغير قطتي = جديد قطة()

اجعل_الحيوان_يتكلم(كلبي)
اجعل_الحيوان_يتكلم(قطتي)

اطبع("--- اختبار استدعاء الأب ---")
صنف كلب_بوليسي يمتد كلب
    دالة تكلم()
        الأب.تكلم()
        اطبع("الكلب البوليسي يراقب المكان")
    نهاية
نهاية

متغير بوليسي = جديد كلب_بوليسي()
بوليسي.تكلم()

اطبع("--- اختبار انتهاء الاختبار بنجاح ---")
