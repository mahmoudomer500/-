# اختبار الأرقام العائمة

متغير رقم1 = 0.0
متغير رقم2 = 0.5
متغير رقم3 = 1.0

اطبع("رقم1 = " + رقم1)
اطبع("رقم2 = " + رقم2)
اطبع("رقم3 = " + رقم3)

# اختبار دالة مع معاملات عائمة
دالة جمع_أرقام(أ, ب):
    ارجع أ + ب
نهاية

متغير النتيجة = جمع_أرقام(1.5, 2.5)
اطبع("1.5 + 2.5 = " + النتيجة)
