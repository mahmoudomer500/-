# اختبار المصفوفات والكائنات

# اختبار مصفوفة بسيطة
متغير أرقام = [1, 2, 3, 4, 5]

اطبع("المصفوفة الأصلية:")
اطبع("أرقام[0] = " + أرقام[0])
اطبع("أرقام[2] = " + أرقام[2])

# تعديل عنصر في المصفوفة
أرقام[1] = 99
اطبع("بعد التعديل:")
اطبع("أرقام[1] = " + أرقام[1])

 اختبار كائن بسيط (إذا كان مدعوماً)
 صنف نقطة
    خاصية x = 0
    خاصية y = 0
 نهاية

#متغير النقطة = جديد نقطة()
 النقطة.x = 10
 النقطة.y = 20

 اطبع("النقطة: (" + النقطة.x + ", " + النقطة.y + ")")

اطبع("اختبار المصفوفات مكتمل!")
