# اختبار الوراثة

# الصنف الأساسي
صنف شكل
    خاصية لون = "أحمر"
نهاية

# صنف يرث من الصنف الأساسي
صنف دائرة يمتد شكل
    خاصية نصف_قطر = 5
نهاية

# صنف يرث من دائرة
صنف كرة يمتد دائرة
    خاصية حجم = 10
نهاية

# إنشاء كائنات
متغير شكلي = جديد شكل()
متغير دائرتي = جديد دائرة()
متغير كرتي = جديد كرة()

اطبع("تم إنشاء جميع الكائنات بنجاح!")
اطبع("الوراثة تعمل بشكل مثالي!")
