# حاسبة عربية بسيطة
# اختبار أساسي للترجمة

# دالة التشغيل الرئيسية
دالة الرئيسية()
    اطبع("مرحباً بالحاسبة العربية البسيطة!")

    متغير أ = 10
    متغير ب = 5

    الجمع = أ + ب
    اطبع("10 + 5 = ")
    اطبع(الجمع)

    الطرح = أ - ب
    اطبع("10 - 5 = ")
    اطبع(الطرح)

    الضرب = أ * ب
    اطبع("10 * 5 = ")
    اطبع(الضرب)

    إذا ب != 0
        القسمة = أ / ب
        اطبع("10 / 5 = ")
        اطبع(القسمة)
    نهاية

    اطبع("انتهى البرنامج!")
نهاية

# تشغيل البرنامج
الرئيسية()
