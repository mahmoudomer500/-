# ملف توضيحي للنظام كائني التوجه في اللغة العربية
# ════════════════════════════════════════════════════════════════

# استيراد مكتبة الرسومات
# (في الواقع، يجب تضمين ملف رسومات.عربي هنا)

اطبع("🎯 مرحباً بك في عالم البرمجة كائنية التوجه العربية!")
اطبع("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━")

# مثال بسيط على استخدام الأصناف والكائنات
دالة مثال_بسيط_للأصناف()

    # إنشاء صنف بسيط للنقاط
    متغير صنف_النقطة = إنشاء_صنف("نقطة", 0)

    # إضافة متغيرات
    أضف_متغير_للصنف(صنف_النقطة, "x", 0.0)
    أضف_متغير_للصنف(صنف_النقطة, "y", 0.0)

    # دالة لحساب المسافة من الأصل
    دالة احسب_المسافة(هذا)
        متغير x = احصل_على_متغير_الكائن(هذا["معرف_الكائن"], "x")
        متغير y = احصل_على_متغير_الكائن(هذا["معرف_الكائن"], "y")
        ارجع الجذر_التربيعي(x*x + y*y)
    نهاية

    أضف_دالة_للصنف(صنف_النقطة, "احسب_المسافة", احسب_المسافة)

    # إنشاء كائنين من صنف النقطة
    متغير نقطة1 = جديد(صنف_النقطة, {3.0, 4.0})
    متغير نقطة2 = جديد(صنف_النقطة, {6.0, 8.0})

    # استخدام الدوال العضو
    متغير مسافة1 = استدعاء_دالة_الكائن(نقطة1, "احسب_المسافة", {})
    متغير مسافة2 = استدعاء_دالة_الكائن(نقطة2, "احسب_المسافة", {})

    اطبع("📍 النقطة الأولى: المسافة = " + مسافة1)
    اطبع("📍 النقطة الثانية: المسافة = " + مسافة2)

نهاية

# تشغيل المثال
مثال_بسيط_للأصناف()

اطبع("")
اطبع("🎉 انتهى التوضيح! النظام كائني التوجه جاهز للاستخدام!")
اطبع("💡 للمزيد من الأمثلة، راجع ملف رسومات.عربي")
