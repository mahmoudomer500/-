
# نواة المترجم العربي - نسخة مبسطة ومحسنة
دالة اختبار_اساسي():
    عدد = 10
    اطبع("اختبار النواة")
    اطبع(عدد)
    أرجع عدد

دالة اختبار_القوة():
    قاعدة = 2
    اس = 3
    نتيجة = قاعدة * قاعدة * قاعدة
    اطبع("نتيجة القوة")
    اطبع(نتيجة)
    أرجع نتيجة

# البرنامج الرئيسي
اطبع("نواة المترجم العربي")
نتيجة_1 = اختبار_اساسي()
نتيجة_2 = اختبار_القوة()

إذا نتيجة_1 > 5:
    اطبع("النواة تعمل")
وإلا:
    اطبع("خطا في النواة")
