# اختبارات شاملة للمترجم العربي
# =================================

اطبع("🧪 بدء الاختبارات الشاملة...")

# اختبار 1: المتغيرات والأرقام
متغير عدد_صحيح = 42
متغير عدد_عائم = 3.14159
متغير نص = "مرحبا بالعالم"

اطبع("✅ اختبار المتغيرات: " + عدد_صحيح + ", " + عدد_عائم + ", " + نص)

# اختبار 2: العمليات الحسابية
متغير جمع = عدد_صحيح + 8
متغير ضرب = عدد_عائم * 2

اطبع("✅ اختبار العمليات: جمع=" + جمع + ", ضرب=" + ضرب)

# اختبار 3: المصفوفات
متغير مصفوفة = [1, 2, 3, 4, 5]
مصفوفة[0] = 99

اطبع("✅ اختبار المصفوفات: " + مصفوفة)

# اختبار 4: الشروط البسيطة
متغير نتيجة_شرط = 0
إذا عدد_صحيح > 40
    نتيجة_شرط = 1
وإلا
    نتيجة_شرط = 0
نهاية

اطبع("✅ اختبار الشروط البسيطة: " + نتيجة_شرط)

# اختبار 5: الشروط المعقدة
متغير نتيجة_معقد = 0
إذا عدد_صحيح > 30
    إذا عدد_صحيح < 50
        نتيجة_معقد = 2
    وإلا
        نتيجة_معقد = 3
    نهاية
وإلا
    نتيجة_معقد = 1
نهاية

اطبع("✅ اختبار الشروط المعقدة: " + نتيجة_معقد)

# اختبار 6: الدوال البسيطة (إذا كانت تعمل)
# دالة اختبار_دالة(معامل)
#     ارجع معامل * 2
# نهاية
#
# متغير نتيجة_دالة = اختبار_دالة(5)
# اطبع("✅ اختبار الدوال: " + نتيجة_دالة)

اطبع("🎉 اكتملت جميع الاختبارات بنجاح!")
اطبع("📊 تم اختبار: متغيرات، عمليات، مصفوفات، شروط")
