# اختبار الدوال - المرحلة 3

# اختبار 1: دالة بسيطة
دالة ألو():
    اطبع "مرحبا من دالة ألو"
نهاية

ألو()

# اختبار 2: دالة بمعامل
دالة حياتك(الاسم):
    اطبع "السلام عليكم " + الاسم
نهاية

حياتك("أحمد")

# اختبار 3: دالة بمعاملات متعددة وقيمة مرجعة
دالة اجمع(أ، ب):
    النتيجة = أ + ب
    ارجع النتيجة
نهاية

متغير المجموع = اجمع(10، 20)
اطبع "المجموع: " + المجموع

# اختبار 4: دالة حسابية
دالة الضرب(س، ص):
    ارجع س * ص
نهاية

متغير الناتج = الضرب(5، 6)
اطبع "الضرب: " + الناتج

اطبع "انتهى الاختبار"