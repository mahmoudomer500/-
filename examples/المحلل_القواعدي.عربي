# المحلل_القواعدي.عربي — ملف جسر للتوافق مع نظام الاستيراد
# يُعيد تصدير الصنف محلل_نحوي تحت الاسم: المحلل_القواعدي
# المسار المتوقع: "اللغة_العربية/المحلل_القواعدي.عربي"

استورد "محلل_نحوي.عربي"

صف المحلل_القواعدي يرث محلل_نحوي:
    عام:
        دالة جديد(رموز):
            هذا.الرموز = رموز
            هذا.الموضع_الحالي = 0
            هذا.رقم_السطر_الحالي = 1
            هذا.الأخطاء = []
نهاية
