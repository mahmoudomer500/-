# اختبار أداء المترجم العربي
# اختبار العمليات الحسابية الأساسية

متغير عداد = 0
متغير مجموع = 0

دالة اختبار_حسابي()
    متغير أ = 1
    متغير ب = 2
    متغير ناتج = أ + ب
    ارجع ناتج
نهاية

دالة اختبار_حلقات()
    متغير عداد_محلي = 0
    لـ متغير أي = 1 إلى 1000
        عداد_محلي = عداد_محلي + 1
    نهاية
    ارجع عداد_محلي
نهاية

دالة اختبار_نصوص()
    متغير نص = "مرحبا بالعالم"
    متغير نص2 = نص + " في لغة البرمجة العربية"
    ارجع نص2
نهاية

دالة اختبار_مصفوفات()
    متغير مصفوفة[10]
    لـ متغير أي = 0 إلى 9
        مصفوفة[أي] = أي * 2
    نهاية
    ارجع مصفوفة[5]
نهاية

# الاختبار الرئيسي
اطبع "بدء اختبار الأداء..."

# اختبار الحسابات
متغير نتيجة_حساب = اختبار_حسابي()
اطبع "نتيجة الحساب: " نتيجة_حساب

# اختبار الحلقات
متغير نتيجة_حلقات = اختبار_حلقات()
اطبع "نتيجة الحلقات: " نتيجة_حلقات

# اختبار النصوص
متغير نتيجة_نصوص = اختبار_نصوص()
اطبع "نتيجة النصوص: " نتيجة_نصوص

# اختبار المصفوفات
متغير نتيجة_مصفوفات = اختبار_مصفوفات()
اطبع "نتيجة المصفوفات: " نتيجة_مصفوفات

اطبع "انتهاء اختبار الأداء!"
