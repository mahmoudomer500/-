# ═══════════════════════════════════════════════════════════
# ⚠️ نظام معالجة الأخطاء المحسن - الإصدار 2.0
# ═══════════════════════════════════════════════════════════
# وصف: نظام شامل لمعالجة الأخطاء مع رسائل عربية واقتراحات الإصلاح
# الميزات: أخطاء عربية، اقتراحات ذكية، تتبع تفاعلي، تصنيف متقدم
# ═══════════════════════════════════════════════════════════

صف نظام_معالجة_الأخطاء_المحسن:
    خاص:
        قاموس_الأخطاء
        قاموس_الاقتراحات
        سجل_الأخطاء
        إعدادات_المعالجة
        إحصائيات_الأخطاء

    عام:
        # ────────────────────────────────────────────────────────
        # 1️⃣  دالة التهيئة والإعداد
        # ────────────────────────────────────────────────────────

        دالة جديد():
            "تهيئة نظام معالجة الأخطاء المحسن"
            هذا.قاموس_الأخطاء = {}
            هذا.قاموس_الاقتراحات = {}
            هذا.سجل_الأخطاء = []
            هذا.إعدادات_المعالجة = {
                "رسائل_عربية": صحيح,
                "اقتراحات_ذكية": صحيح,
                "تتبع_تفاعلي": صحيح,
                "إحصائيات_مفصلة": صحيح,
                "تصدير_الأخطاء": خطأ
            }
            هذا.إحصائيات_الأخطاء = {
                "إجمالي_الأخطاء": 0,
                "أخطاء_حرجة": 0,
                "أخطاء_تحذير": 0,
                "أخطاء_معلوماتية": 0,
                "أخطاء_مُصلحة": 0
            }
            هذا.تهيئة_قاموس_الأخطاء()
            هذا.تهيئة_قاموس_الاقتراحات()

        دالة تهيئة_قاموس_الأخطاء():
            "تهيئة قاموس الأخطاء مع الرسائل العربية"
            # أخطاء تركيبية
            هذا.قاموس_الأخطاء["تركيبي_مفقود_فاصلة"] = {
                "النوع": "تركيبي",
                "المستوى": "خطأ",
                "الكود": "SYN001",
                "الرسالة_العربية": "خطأ تركيبي: فاصلة مفقودة بعد المتغير '{متغير}' في السطر {سطر}",
                "الرسالة_الإنجليزية": "Syntax Error: Missing comma after variable '{variable}' at line {line}",
                "الأسباب_المحتملة": ["نسيان الفاصلة", "خطأ في كتابة المصفوفة", "مشكلة في التنسيق"]
            }

            هذا.قاموس_الأخطاء["تركيبي_قوس_غير_متوازن"] = {
                "النوع": "تركيبي",
                "المستوى": "خطأ",
                "الكود": "SYN002",
                "الرسالة_العربية": "خطأ تركيبي: قوس '{قوس}' غير متوازن في السطر {سطر}",
                "الرسالة_الإنجليزية": "Syntax Error: Unbalanced '{bracket}' at line {line}",
                "الأسباب_المحتملة": ["قوس مفتوح بدون إغلاق", "قوس مغلق بدون فتح", "خطأ في التداخل"]
            }

            هذا.قاموس_الأخطاء["تركيبي_كلمة_محجوزة"] = {
                "النوع": "تركيبي",
                "المستوى": "خطأ",
                "الكود": "SYN003",
                "الرسالة_العربية": "خطأ تركيبي: '{كلمة}' كلمة محجوزة ولا يمكن استخدامها كاسم متغير",
                "الرسالة_الإنجليزية": "Syntax Error: '{word}' is a reserved keyword and cannot be used as variable name",
                "الأسباب_المحتملة": ["استخدام كلمة محجوزة كاسم", "خطأ إملائي", "تضارب في التسمية"]
            }

            # أخطاء دلالية
            هذا.قاموس_الأخطاء["دلالي_متغير_غير_معرف"] = {
                "النوع": "دلالي",
                "المستوى": "خطأ",
                "الكود": "SEM001",
                "الرسالة_العربية": "خطأ دلالي: المتغير '{متغير}' غير معرف في السطر {سطر}",
                "الرسالة_الإنجليزية": "Semantic Error: Variable '{variable}' is not defined at line {line}",
                "الأسباب_المحتملة": ["خطأ إملائي في الاسم", "نسيان تعريف المتغير", "خارج نطاق المتغير"]
            }

            هذا.قاموس_الأخطاء["دلالي_نوع_غير_متوافق"] = {
                "النوع": "دلالي",
                "المستوى": "خطأ",
                "الكود": "SEM002",
                "الرسالة_العربية": "خطأ دلالي: لا يمكن تعيين قيمة من نوع '{نوع_المصدر}' إلى متغير من نوع '{نوع_الهدف}'",
                "الرسالة_الإنجليزية": "Semantic Error: Cannot assign value of type '{source_type}' to variable of type '{target_type}'",
                "الأسباب_المحتملة": ["تحويل نوع غير مدعوم", "عدم تطابق الأنواع", "خطأ في منطق البرنامج"]
            }

            هذا.قاموس_الأخطاء["دلالي_دالة_غير_موجودة"] = {
                "النوع": "دلالي",
                "المستوى": "خطأ",
                "الكود": "SEM003",
                "الرسالة_العربية": "خطأ دلالي: الدالة '{دالة}' غير موجودة أو غير قابلة للوصول",
                "الرسالة_الإنجليزية": "Semantic Error: Function '{function}' does not exist or is not accessible",
                "الأسباب_المحتملة": ["خطأ إملائي في اسم الدالة", "نسيان استيراد الدالة", "دالة غير مُصدّرة"]
            }

            # أخطاء وقت التشغيل
            هذا.قاموس_الأخطاء["تشغيل_قسمة_على_صفر"] = {
                "النوع": "تشغيل",
                "المستوى": "خطأ",
                "الكود": "RUN001",
                "الرسالة_العربية": "خطأ وقت التشغيل: قسمة على صفر في السطر {سطر}",
                "الرسالة_الإنجليزية": "Runtime Error: Division by zero at line {line}",
                "الأسباب_المحتملة": ["متغير يساوي صفر", "خطأ في الحسابات", "عدم التحقق من القيمة"]
            }

            هذا.قاموس_الأخطاء["تشغيل_فهرس_خارج_النطاق"] = {
                "النوع": "تشغيل",
                "المستوى": "خطأ",
                "الكود": "RUN002",
                "الرسالة_العربية": "خطأ وقت التشغيل: فهرس {فهرس} خارج نطاق المصفوفة (الحجم: {حجم})",
                "الرسالة_الإنجليزية": "Runtime Error: Index {index} out of bounds (size: {size})",
                "الأسباب_المحتملة": ["فهرس أكبر من حجم المصفوفة", "فهرس سالب", "خطأ في الحلقات"]
            }

            هذا.قاموس_الأخطاء["تشغيل_ذاكرة_غير_كافية"] = {
                "النوع": "تشغيل",
                "المستوى": "خطأ",
                "الكود": "RUN003",
                "الرسالة_العربية": "خطأ وقت التشغيل: ذاكرة غير كافية للعملية المطلوبة",
                "الرسالة_الإنجليزية": "Runtime Error: Insufficient memory for the requested operation",
                "الأسباب_المحتملة": ["استهلاك ذاكرة عالي", "تسرب ذاكرة", "عملية تحتاج موارد كبيرة"]
            }

            # تحذيرات
            هذا.قاموس_الأخطاء["تحذير_متغير_غير_مستخدم"] = {
                "النوع": "تحذير",
                "المستوى": "تحذير",
                "الكود": "WRN001",
                "الرسالة_العربية": "تحذير: المتغير '{متغير}' معرف لكنه غير مستخدم",
                "الرسالة_الإنجليزية": "Warning: Variable '{variable}' is defined but never used",
                "الأسباب_المحتملة": ["كود متروك", "خطأ في التسمية", "منطق غير مكتمل"]
            }

            هذا.قاموس_الأخطاء["تحذير_دالة_طويلة"] = {
                "النوع": "تحذير",
                "المستوى": "تحذير",
                "الكود": "WRN002",
                "الرسالة_العربية": "تحذير: الدالة '{دالة}' طويلة جداً ({سطور} سطر) - يُفضل تقسيمها",
                "الرسالة_الإنجليزية": "Warning: Function '{function}' is too long ({lines} lines) - consider splitting",
                "الأسباب_المحتملة": ["دالة متعددة المهام", "منطق معقد", "عدم استخراج الدوال المساعدة"]
            }

            هذا.قاموس_الأخطاء["تحذير_كود_مكرر"] = {
                "النوع": "تحذير",
                "المستوى": "تحذير",
                "الكود": "WRN003",
                "الرسالة_العربية": "تحذير: كود مكرر مكتشف في الأسطر {سطور} - يُفضل استخراج دالة",
                "الرسالة_الإنجليزية": "Warning: Duplicate code detected at lines {lines} - consider extracting function",
                "الأسباب_المحتملة": ["نسخ ولصق الكود", "منطق متكرر", "عدم إعادة استخدام الدوال"]
            }

        دالة تهيئة_قاموس_الاقتراحات():
            "تهيئة قاموس الاقتراحات للإصلاح"
            # اقتراحات للأخطاء التركيبية
            هذا.قاموس_الاقتراحات["تركيبي_مفقود_فاصلة"] = [
                "أضف فاصلة (،) بعد المتغير المحدد",
                "تحقق من تنسيق المصفوفات والقواميس",
                "استخدم فحص البناء التلقائي لتجنب هذه الأخطاء"
            ]

            هذا.قاموس_الاقتراحات["تركيبي_قوس_غير_متوازن"] = [
                "تحقق من عدد الأقواس المفتوحة والمغلقة",
                "استخدم محرر كود مع تظليل البناء التركيبي",
                "قم بمراجعة التداخل في الشروط والحلقات"
            ]

            هذا.قاموس_الاقتراحات["تركيبي_كلمة_محجوزة"] = [
                "غير اسم المتغير ليكون مختلف عن الكلمات المحجوزة",
                "استخدم قائمة الكلمات المحجوزة كمرجع",
                "أضف بادئة أو لاحقة للتمييز عن الكلمات المحجوزة"
            ]

            # اقتراحات للأخطاء الدلالية
            هذا.قاموس_الاقتراحات["دلالي_متغير_غير_معرف"] = [
                "تحقق من الإملاء الصحيح لاسم المتغير",
                "تأكد من تعريف المتغير قبل استخدامه",
                "تحقق من نطاق المتغير (local/global)",
                "استخدم أدوات البحث في الكود للعثور على المتغير"
            ]

            هذا.قاموس_الاقتراحات["دلالي_نوع_غير_متوافق"] = [
                "استخدم تحويل نوع صريح إذا لزم الأمر",
                "تحقق من توافق الأنواع في العمليات",
                "استخدم متغيرات وسيطة لتحويل الأنواع",
                "راجع توثيق أنواع البيانات المدعومة"
            ]

            هذا.قاموس_الاقتراحات["دلالي_دالة_غير_موجودة"] = [
                "تحقق من اسم الدالة وإملائها",
                "تأكد من استيراد الدالة أو المكتبة المطلوبة",
                "تحقق من توفر الدالة في الإصدار الحالي",
                "استخدم قائمة الدوال المتاحة كمرجع"
            ]

            # اقتراحات لأخطاء وقت التشغيل
            هذا.قاموس_الاقتراحات["تشغيل_قسمة_على_صفر"] = [
                "أضف فحص للقيمة قبل القسمة: إذا قيمة != 0",
                "استخدم try/except لمعالجة هذا الخطأ",
                "قم بتعيين قيمة افتراضية في حالة القسمة على صفر"
            ]

            هذا.قاموس_الاقتراحات["تشغيل_فهرس_خارج_النطاق"] = [
                "أضف فحص لحدود الفهرس قبل الوصول",
                "استخدم حلقات آمنة مع فحص الحجم",
                "تحقق من صحة الفهرس المدخل من المستخدم"
            ]

            هذا.قاموس_الاقتراحات["تشغيل_ذاكرة_غير_كافية"] = [
                "قم بتحسين استخدام الذاكرة في البرنامج",
                "استخدم معالجة البيانات على دفعات",
                "زيادة ذاكرة النظام أو استخدام ملفات مؤقتة"
            ]

            # اقتراحات للتحذيرات
            هذا.قاموس_الاقتراحات["تحذير_متغير_غير_مستخدم"] = [
                "احذف المتغير إذا لم يعد مطلوباً",
                "استخدم المتغير في منطق البرنامج",
                "أضف تعليق يوضح سبب الاحتفاظ بالمتغير"
            ]

            هذا.قاموس_الاقتراحات["تحذير_دالة_طويلة"] = [
                "قسم الدالة إلى دوال أصغر وأكثر تحديداً",
                "استخرج الأجزاء المعقدة إلى دوال منفصلة",
                "استخدم كلاسات لتجميع الوظائف ذات الصلة"
            ]

            هذا.قاموس_الاقتراحات["تحذير_كود_مكرر"] = [
                "استخرج الكود المكرر إلى دالة منفصلة",
                "استخدم وراثة أو تكوين لتجنب التكرار",
                "أنشئ دوال مساعدة للعمليات الشائعة"
            ]

        # ────────────────────────────────────────────────────────
        # 2️⃣  دوال معالجة الأخطاء الأساسية
        # ────────────────────────────────────────────────────────

        دالة إثارة_خطأ(كود_الخطأ، المعاملات = {}):
            "إثارة خطأ مع رسالة عربية واقتراحات الإصلاح"
            إذا ليس (كود_الخطأ في مفاتيح(هذا.قاموس_الأخطاء)):
                هذا.إثارة_خطأ_عام("كود الخطأ غير معروف: " + كود_الخطأ)
                أرجع

            معلومات_الخطأ = هذا.قاموس_الأخطاء[كود_الخطأ]

            # إنشاء رسالة الخطأ
            رسالة_الخطأ = هذا.إنشاء_رسالة_خطأ(معلومات_الخطأ, المعاملات)

            # تسجيل الخطأ
            هذا.تسجيل_خطأ(كود_الخطأ, رسالة_الخطأ, معلومات_الخطأ)

            # عرض الخطأ للمطور
            هذا.عرض_خطأ_للمطور(معلومات_الخطأ, رسالة_الخطأ, كود_الخطأ)

            # تحديث الإحصائيات
            هذا.تحديث_إحصائيات_الأخطاء(معلومات_الخطأ["المستوى"])

        دالة إثارة_خطأ_عام(رسالة):
            "إثارة خطأ عام بدون كود محدد"
            اطبع("❌ خطأ عام: " + رسالة)
            هذا.تسجيل_خطأ("GENERAL", رسالة, {"المستوى": "خطأ"})
            هذا.تحديث_إحصائيات_الأخطاء("خطأ")

        دالة إنشاء_رسالة_خطأ(معلومات_الخطأ، المعاملات):
            "إنشاء رسالة خطأ مخصصة بالمعاملات"
            رسالة = معلومات_الخطأ["الرسالة_العربية"]

            # استبدال العناصر النائبة
            لكل مفتاح في مفاتيح(المعاملات):
                قيمة = نص(المعاملات[مفتاح])
                رسالة = استبدال(رسالة, "{" + مفتاح + "}", قيمة)

            أرجع رسالة

        دالة عرض_خطأ_للمطور(معلومات_الخطأ، رسالة_الخطأ، كود_الخطأ):
            "عرض الخطأ للمطور مع التفاصيل والاقتراحات"
            # تحديد رمز الخطأ
            رمز = هذا.حصول_على_رمز_الخطأ(معلومات_الخطأ["المستوى"])

            # عرض العنوان
            اطبع("")
            اطبع("=" * 60)
            اطبع(رمز + " " + معلومات_الخطأ["النوع"] + " - " + كود_الخطأ)
            اطبع("=" * 60)

            # عرض رسالة الخطأ
            اطبع("الرسالة: " + رسالة_الخطأ)

            # عرض الأسباب المحتملة
            إذا معلومات_الخطأ["الأسباب_المحتملة"]:
                اطبع("\\nالأسباب المحتملة:")
                لكل سبب في معلومات_الخطأ["الأسباب_المحتملة"]:
                    اطبع("• " + سبب)

            # عرض اقتراحات الإصلاح
            اقتراحات = هذا.قاموس_الاقتراحات[كود_الخطأ]
            إذا اقتراحات:
                اطبع("\\nاقتراحات الإصلاح:")
                لكل اقتراح في اقتراحات:
                    اطبع("💡 " + اقتراح)

            # عرض مساعدة إضافية
            اطبع("\\nمساعدة إضافية:")
            اطبع("• استخدم F1 للحصول على تعليمات مفصلة")
            اطبع("• راجع التوثيق في docs/errors/" + كود_الخطأ + ".md")
            اطبع("• انضم إلى مجتمع المطورين للمساعدة")

            اطبع("=" * 60)

        دالة تسجيل_خطأ(كود_الخطأ، رسالة_الخطأ، معلومات_الخطأ):
            "تسجيل الخطأ في سجل الأخطاء"
            إدخال_السجل = {
                "الوقت": الوقت_الحالي(),
                "الكود": كود_الخطأ,
                "الرسالة": رسالة_الخطأ,
                "النوع": معلومات_الخطأ["النوع"],
                "المستوى": معلومات_الخطأ["المستوى"],
                "الحالة": "نشط"
            }

            هذا.سجل_الأخطاء.أضف(إدخال_السجل)

        دالة تحديث_إحصائيات_الأخطاء(مستوى_الخطأ):
            "تحديث إحصائيات الأخطاء"
            هذا.إحصائيات_الأخطاء["إجمالي_الأخطاء"] = هذا.إحصائيات_الأخطاء["إجمالي_الأخطاء"] + 1

            حسب مستوى_الخطأ:
                حالة "خطأ":
                    هذا.إحصائيات_الأخطاء["أخطاء_حرجة"] = هذا.إحصائيات_الأخطاء["أخطاء_حرجة"] + 1
                حالة "تحذير":
                    هذا.إحصائيات_الأخطاء["أخطاء_تحذير"] = هذا.إحصائيات_الأخطاء["أخطاء_تحذير"] + 1
                حالة "معلومات":
                    هذا.إحصائيات_الأخطاء["أخطاء_معلوماتية"] = هذا.إحصائيات_الأخطاء["أخطاء_معلوماتية"] + 1

        دالة حل_خطأ(كود_الخطأ, حل_مطبق):
            "تسجيل حل خطأ"
            لكل خطأ في هذا.سجل_الأخطاء:
                إذا خطأ["الكود"] == كود_الخطأ و خطأ["الحالة"] == "نشط":
                    خطأ["الحالة"] = "مُحل"
                    خطأ["الحل"] = حل_مطبق
                    خطأ["وقت_الحل"] = الوقت_الحالي()
                    هذا.إحصائيات_الأخطاء["أخطاء_مُصلحة"] = هذا.إحصائيات_الأخطاء["أخطاء_مُصلحة"] + 1
                    أرجع صحيح

            أرجع خطأ

        # ────────────────────────────────────────────────────────
        # 3️⃣  دوال المساعدة والأدوات
        # ────────────────────────────────────────────────────────

        دالة الحصول_على_رمز_الخطأ(مستوى):
            "الحصول على رمز الخطأ حسب المستوى"
            حسب مستوى:
                حالة "خطأ":
                    أرجع "❌"
                حالة "تحذير":
                    أرجع "⚠️"
                حالة "معلومات":
                    أرجع "ℹ️"
                آخر:
                    أرجع "❓"

        دالة البحث_عن_خطأ(كود_الخطأ):
            "البحث عن معلومات خطأ محدد"
            أرجع هذا.قاموس_الأخطاء[كود_الخطأ] أو {}

        دالة الحصول_على_اقتراحات(كود_الخطأ):
            "الحصول على اقتراحات الإصلاح لخطأ محدد"
            أرجع هذا.قاموس_الاقتراحات[كود_الخطأ] أو []

        دالة تصدير_سجل_الأخطاء(مسار_الملف):
            "تصدير سجل الأخطاء إلى ملف"
            محتوى = "# سجل الأخطاء المفصل\\n\\n"

            لكل خطأ في هذا.سجل_الأخطاء:
                محتوى = محتوى + "الوقت: " + نص(خطأ["الوقت"]) + "\\n"
                محتوى = محتوى + "الكود: " + خطأ["الكود"] + "\\n"
                محتوى = محتوى + "النوع: " + خطأ["النوع"] + "\\n"
                محتوى = محتوى + "المستوى: " + خطأ["المستوى"] + "\\n"
                محتوى = محتوى + "الرسالة: " + خطأ["الرسالة"] + "\\n"
                محتوى = محتوى + "الحالة: " + خطأ["الحالة"] + "\\n"

                إذا خطأ["الحل"]:
                    محتوى = محتوى + "الحل: " + خطأ["الحل"] + "\\n"

                محتوى = محتوى + "---\\n"

            كتابة_ملف(مسار_الملف, محتوى)
            اطبع("✅ تم تصدير سجل الأخطاء إلى: " + مسار_الملف)

        دالة إنشاء_تقرير_الأخطاء():
            "إنشاء تقرير شامل عن الأخطاء"
            تقرير = """
# ════════════════════════════════════════════════
# 📊 تقرير الأخطاء الشامل
# ════════════════════════════════════════════════
# التاريخ: {التاريخ}
# ════════════════════════════════════════════════

## إحصائيات الأخطاء:
• إجمالي الأخطاء: {إجمالي_الأخطاء}
• أخطاء حرجة: {أخطاء_حرجة}
• أخطاء تحذير: {أخطاء_تحذير}
• أخطاء معلوماتية: {أخطاء_معلوماتية}
• أخطاء مُصلحة: {أخطاء_مُصلحة}

## معدل النجاح في الإصلاح:
{معدل_الإصلاح}%

## تصنيف الأخطاء الأكثر شيوعاً:
{الأخطاء_الشائعة}

═════════════════════════════════════════════════
"""

            # حساب معدل الإصلاح
            معدل_الإصلاح = 0
            إذا هذا.إحصائيات_الأخطاء["إجمالي_الأخطاء"] > 0:
                معدل_الإصلاح = (هذا.إحصائيات_الأخطاء["أخطاء_مُصلحة"] / هذا.إحصائيات_الأخطاء["إجمالي_الأخطاء"]) * 100

            # تحليل الأخطاء الشائعة
            الأخطاء_الشائعة = هذا.تحليل_الأخطاء_الشائعة()

            # استبدال القيم
            تقرير = استبدال(تقرير, "{التاريخ}", التاريخ_والوقت_الحالي())
            تقرير = استبدال(تقرير, "{إجمالي_الأخطاء}", نص(هذا.إحصائيات_الأخطاء["إجمالي_الأخطاء"]))
            تقرير = استبدال(تقرير, "{أخطاء_حرجة}", نص(هذا.إحصائيات_الأخطاء["أخطاء_حرجة"]))
            تقرير = استبدال(تقرير, "{أخطاء_تحذير}", نص(هذا.إحصائيات_الأخطاء["أخطاء_تحذير"]))
            تقرير = استبدال(تقرير, "{أخطاء_معلوماتية}", نص(هذا.إحصائيات_الأخطاء["أخطاء_معلوماتية"]))
            تقرير = استبدال(تقرير, "{أخطاء_مُصلحة}", نص(هذا.إحصائيات_الأخطاء["أخطاء_مُصلحة"]))
            تقرير = استبدال(تقرير, "{معدل_الإصلاح}", نص_دقة(معدل_الإصلاح, 1))
            تقرير = استبدال(تقرير, "{الأخطاء_الشائعة}", الأخطاء_الشائعة)

            أرجع تقرير

        دالة تحليل_الأخطاء_الشائعة():
            "تحليل الأخطاء الأكثر شيوعاً"
            عد_الأخطاء = {}

            لكل خطأ في هذا.سجل_الأخطاء:
                كود = خطأ["الكود"]
                عد_الأخطاء[كود] = (عد_الأخطاء[كود] أو 0) + 1

            # ترتيب حسب التكرار
            مراتب = []
            لكل كود في مفاتيح(عد_الأخطاء):
                مراتب.أضف({"الكود": كود, "العدد": عد_الأخطاء[كود]})

            # ترتيب تنازلي
            مراتب = رتب(مراتب, lambda a, b: a["العدد"] > b["العدد"])

            نص_النتائج = ""
            لكل عنصر في مراتب[0..أصغر(5, طول(مراتب))-1]:
                معلومات_الخطأ = هذا.قاموس_الأخطاء[عنصر["الكود"]]
                إذا معلومات_الخطأ:
                    نص_النتائج = نص_النتائج + "• " + معلومات_الخطأ["النوع"] + " (" + عنصر["الكود"] + "): " + نص(عنصر["العدد"]) + " مرة\\n"

            أرجع نص_النتائج أو "لا توجد أخطاء مسجلة"

        # ────────────────────────────────────────────────────────
        # 4️⃣  دوال التفاعل والمساعدة
        # ────────────────────────────────────────────────────────

        دالة عرض_مساعدة_خطأ(كود_الخطأ):
            "عرض مساعدة مفصلة لخطأ محدد"
            معلومات_الخطأ = هذا.البحث_عن_خطأ(كود_الخطأ)
            اقتراحات = هذا.الحصول_على_اقتراحات(كود_الخطأ)

            إذا معلومات_الخطأ["الكود"]:
                اطبع("\\n=== مساعدة للخطأ: " + كود_الخطأ + " ===")
                اطبع("النوع: " + معلومات_الخطأ["النوع"])
                اطبع("المستوى: " + معلومات_الخطأ["المستوى"])
                اطبع("الوصف: " + معلومات_الخطأ["الرسالة_العربية"])

                إذا اقتراحات:
                    اطبع("\\nالحلول المقترحة:")
                    لكل اقتراح في اقتراحات:
                        اطبع("• " + اقتراح)

                اطبع("\\nللمزيد من المساعدة، راجع:")
                اطبع("docs/errors/" + كود_الخطأ + ".md")
            وإلا:
                اطبع("❌ كود الخطأ غير معروف: " + كود_الخطأ)

        دالة تشخيص_تلقائي(كود_المصدر):
            "تشخيص تلقائي للأخطاء في الكود"
            اطبع("🔍 بدء التشخيص التلقائي...")

            مشاكل_مكتشفة = []

            # فحص أساسيات
            مشاكل_مكتشفة.أضف_كل(هذا.فحص_الأقواس(كود_المصدر))
            مشاكل_مكتشفة.أضف_كل(هذا.فحص_المتغيرات_غير_المعرفة(كود_المصدر))
            مشاكل_مكتشفة.أضف_كل(هذا.فحص_الدوال_الطويلة(كود_المصدر))

            إذا طول(مشاكل_مكتشفة) > 0:
                اطبع("⚠️ تم العثور على " + نص(طول(مشاكل_مكتشفة)) + " مشكلة محتملة:")
                لكل مشكلة في مشاكل_مكتشفة:
                    اطبع("• " + مشكلة)
            وإلا:
                اطبع("✅ لم يتم العثور على مشاكل واضحة في الكود")

            أرجع مشاكل_مكتشفة

        # دوال فحص مساعدة
        دالة فحص_الأقواس(كود):
            مشاكل = []
            أقواس_مفتوحة = 0
            لكل حرف في كود:
                إذا حرف == "{":
                    أقواس_مفتوحة = أقواس_مفتوحة + 1
                وإلا إذا حرف == "}":
                    أقواس_مفتوحة = أقواس_مفتوحة - 1
                    إذا أقواس_مفتوحة < 0:
                        مشاكل.أضف("قوس إغلاق بدون فتح")
                        أقواس_مفتوحة = 0

            إذا أقواس_مفتوحة > 0:
                مشاكل.أضف("قوس فتح بدون إغلاق")

            أرجع مشاكل

        دالة فحص_المتغيرات_غير_المعرفة(كود):
            # فحص بسيط - في التطبيق الكامل سيكون أكثر تعقيداً
            أرجع []

        دالة فحص_الدوال_الطويلة(كود):
            مشاكل = []
            سطور = تقسيم(كود, "\\n")

            لكل رقم_سطر في 0..طول(سطور)-1:
                سطر = سطور[رقم_سطر]
                إذا يحتوي(سطر, "دالة "):
                    # عد السطور حتى نهاية الدالة (تبسيط)
                    عدد_سطور_الدالة = 5  # قيمة تجريبية
                    إذا عدد_سطور_الدالة > 20:
                        مشاكل.أضف("دالة طويلة في السطر " + نص(رقم_سطر + 1))

            أرجع مشاكل

        # ────────────────────────────────────────────────────────
        # 5️⃣  دوال الإحصائيات والتقارير
        # ────────────────────────────────────────────────────────

        دالة عرض_الإحصائيات():
            "عرض إحصائيات نظام معالجة الأخطاء"
            اطبع("""
# ════════════════════════════════════════════════
# 📊 إحصائيات معالجة الأخطاء
# ════════════════════════════════════════════════
إجمالي الأخطاء المسجلة: {إجمالي_الأخطاء}
أخطاء حرجة: {أخطاء_حرجة}
أخطاء تحذير: {أخطاء_تحذير}
أخطاء معلوماتية: {أخطاء_معلوماتية}
أخطاء مُصلحة: {أخطاء_مُصلحة}

معدل الإصلاح: {معدل_الإصلاح}%
عدد أنواع الأخطاء المدعومة: {أنواع_الأخطاء}

═════════════════════════════════════════════════
""".استبدال("{إجمالي_الأخطاء}", نص(هذا.إحصائيات_الأخطاء["إجمالي_الأخطاء"]))
  .استبدال("{أخطاء_حرجة}", نص(هذا.إحصائيات_الأخطاء["أخطاء_حرجة"]))
  .استبدال("{أخطاء_تحذير}", نص(هذا.إحصائيات_الأخطاء["أخطاء_تحذير"]))
  .استبدال("{أخطاء_معلوماتية}", نص(هذا.إحصائيات_الأخطاء["أخطاء_معلوماتية"]))
  .استبدال("{أخطاء_مُصلحة}", نص(هذا.إحصائيات_الأخطاء["أخطاء_مُصلحة"]))
  .استبدال("{معدل_الإصلاح}", نص_دقة((هذا.إحصائيات_الأخطاء["أخطاء_مُصلحة"] / أكبر(هذا.إحصائيات_الأخطاء["إجمالي_الأخطاء"], 1)) * 100, 1))
  .استبدال("{أنواع_الأخطاء}", نص(طول(مفاتيح(هذا.قاموس_الأخطاء)))))

        دالة حفظ_الإعدادات():
            "حفظ إعدادات نظام معالجة الأخطاء"
            محتوى = """
# إعدادات نظام معالجة الأخطاء
رسائل_عربية = {رسائل_عربية}
اقتراحات_ذكية = {اقتراحات_ذكية}
تتبع_تفاعلي = {تتبع_تفاعلي}
إحصائيات_مفصلة = {إحصائيات_مفصلة}
تصدير_الأخطاء = {تصدير_الأخطاء}
"""

            محتوى = استبدال(محتوى, "{رسائل_عربية}", نص(هذا.إعدادات_المعالجة["رسائل_عربية"]))
            محتوى = استبدال(محتوى, "{اقتراحات_ذكية}", نص(هذا.إعدادات_المعالجة["اقتراحات_ذكية"]))
            محتوى = استبدال(محتوى, "{تتبع_تفاعلي}", نص(هذا.إعدادات_المعالجة["تتبع_تفاعلي"]))
            محتوى = استبدال(محتوى, "{إحصائيات_مفصلة}", نص(هذا.إعدادات_المعالجة["إحصائيات_مفصلة"]))
            محتوى = استبدال(محتوى, "{تصدير_الأخطاء}", نص(هذا.إعدادات_المعالجة["تصدير_الأخطاء"]))

            كتابة_ملف("إعدادات_معالجة_الأخطاء.conf", محتوى)

# ═══════════════════════════════════════════════════════════
# 🎯 أمثلة على الاستخدام
# ═══════════════════════════════════════════════════════════

# إنشاء نظام معالجة الأخطاء
نظام_الأخطاء = نظام_معالجة_الأخطاء_المحسن.جديد()

# مثال 1: إثارة خطأ تركيبي
نظام_الأخطاء.إثارة_خطأ("تركيبي_مفقود_فاصلة", {
    "متغير": "اسم",
    "سطر": 15
})

# مثال 2: إثارة خطأ دلالي
نظام_الأخطاء.إثارة_خطأ("دلالي_متغير_غير_معرف", {
    "متغير": "عداد",
    "سطر": 22
})

# مثال 3: إثارة خطأ وقت تشغيل
نظام_الأخطاء.إثارة_خطأ("تشغيل_فهرس_خارج_النطاق", {
    "فهرس": 10,
    "حجم": 5
})

# عرض المساعدة لخطأ محدد
نظام_الأخطاء.عرض_مساعدة_خطأ("تركيبي_قوس_غير_متوازن")

# تشخيص تلقائي للكود
كود_تجريبي = """
دالة مثال()
    متغير = 10
    إذا متغير > 5 {
        اطبع("كبير")
    # مفقود قوس الإغلاق
    اطبع("انتهى")
"""

مشاكل = نظام_الأخطاء.تشخيص_تلقائي(كود_تجريبي)

# عرض الإحصائيات
نظام_الأخطاء.عرض_الإحصائيات()

# ═══════════════════════════════════════════════════════════