# اختبار بسيط لدالة أضف مع إعادة التخصيص

دالة رئيسية():
    اطبع("=== اختبار دالة أضف البسيط ===")

    # إنشاء مصفوفة فارغة
    مصفوفة_اختبار = []

    # إضافة عناصر
    أضف(مصفوفة_اختبار, 100)
    أضف(مصفوفة_اختبار, 200)
    أضف(مصفوفة_اختبار, 300)

    # التحقق من النتائج
    إذا مصفوفة_اختبار[0] == 100 و مصفوفة_اختبار[1] == 200 و مصفوفة_اختبار[2] == 300:
        اطبع("✅ دالة أضف تعمل بشكل صحيح!")
    وإلا:
        اطبع("❌ مشكلة في دالة أضف")

    اطبع("=== نهاية الاختبار ===")