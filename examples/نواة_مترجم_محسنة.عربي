# نواة المترجم العربي المحسنة
س = 42
اطبع("النواة الأساسية تعمل")
اطبع(س)

دالة اختبار():
    عدد = 10
    اطبع("اختبار الدالة")
    اطبع(عدد)
    أرجع عدد

نتيجة = اختبار()
إذا نتيجة > 5:
    اطبع("النواة تعمل بشكل صحيح")
وإلا:
    اطبع("خطأ في النواة")
نهاية

اطبع("اكتمل اختبار النواة")